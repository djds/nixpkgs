diff --git a/data/start-cosmic b/data/start-cosmic
index 726b4ee..2a3b15e 100755
--- a/data/start-cosmic
+++ b/data/start-cosmic
@@ -49,13 +49,10 @@ if [ -d "/run/user/$(id -u)/keyring" ]; then
 
     # Use PATH lookup instead of hardcoding /usr/bin
     if command -v gnome-keyring-daemon >/dev/null 2>&1; then
-        eval "$(gnome-keyring-daemon --start --components=pkcs11,secrets,ssh > /dev/null 2>&1)"
+        eval "$(gnome-keyring-daemon --start --components=pkcs11,secrets > /dev/null 2>&1)"
     else
         echo "gnome-keyring-daemon not found in PATH" >&2
     fi
-
-    # Set SSH_AUTH_SOCK to the standard gnome-keyring socket
-    export SSH_AUTH_SOCK="/run/user/$(id -u)/keyring/ssh"
 fi
 
 # Run cosmic-session
